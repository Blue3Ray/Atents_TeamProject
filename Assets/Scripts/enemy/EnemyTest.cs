using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//임시 스크립트 제작 추후 주석 없앨 예정
public class EnemyTest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
 //       Hp_Bar = Instantiate(prfBpBar, Canvas.transform).GetComponent<RectTransform>();
    }

    // Update is called once per frame
    void Update()
    {
   //     Vector3 _hpBarPos =
  //          Camera.main.WorldToScreenPoint(new Vector3(transform.position.x, transform.position.y + height, 0));
  //      _hpBarPos.position = _hpBarPos;
 
    }
  //  private void SetEnemyStatus(string _enemyName, int _maxHp, int _atDmg, int_atkSpeed) 
    {
   //     enemyName = _enemyName;
  //      maxHp = _maxHp;
   //     nowHp = _maxHp;
  //      atkDmg = _atkDmg;
   //     atkSpeed = _atkSpeed;
    
    }

  //  public EnemyBase;
        private void OnRenderImage(RenderTexture source, RenderTexture destination)
    {
//        nowHpbar;
    }

}
